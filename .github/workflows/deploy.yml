name: Deploy
on: [push]
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      - name: Restart app
        run: |
          pkill -f 'python3' || true
          cd /home/admn/sigamiz && git pull && ./start.sh &