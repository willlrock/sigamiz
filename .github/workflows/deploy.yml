name: Deploy
on: [push]
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      - name: Restart app
        run: |
          cd /home/admn/sigamiz && git pull
          sudo systemctl restart klapa-bot klapa-backend
